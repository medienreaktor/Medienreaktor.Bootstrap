prototype(Medienreaktor.Bootstrap:Content.Code) < prototype(Medienreaktor.Bootstrap:Component) {
    tagName = 'code'
    className = false
    content = ''

    renderer = afx`
        <pre>
            <Medienreaktor.Bootstrap:Augmenter id={props.id} className={props.className}>
                <Neos.Fusion:Tag tagName={props.tagName}>
                    {props.content}
                </Neos.Fusion:Tag>
            </Medienreaktor.Bootstrap:Augmenter>
        </pre>
    `
}
