prototype(Medienreaktor.Bootstrap:Content.Lead) < prototype(Medienreaktor.Bootstrap:Component) {
    tagName = 'p'
    className = 'lead'
    content = ''

    renderer = afx`
        <Medienreaktor.Bootstrap:Augmenter id={props.id} className={props.className}>
            <Neos.Fusion:Tag tagName={props.tagName}>
                {props.content}
            </Neos.Fusion:Tag>
        </Medienreaktor.Bootstrap:Augmenter>
    `
}
